/**
 * Returns a header element for the site
 * @returns {JSX.Element}
 * @constructor
 */
export default function Header() {
    return (
        <h1 className="heading-main-page">John Johnsons Cruisey Movie Monday</h1>
    );
}
